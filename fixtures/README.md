# Fixtures

Anonymised text extracted from real statements and receipts, produced with the `fixtures` skill. Never a PDF, never real IBANs, names or card numbers. Each `<name>.txt` has a `<name>.expected.json` with what the parser must produce.
